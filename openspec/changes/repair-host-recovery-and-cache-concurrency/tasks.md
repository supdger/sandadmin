## 1. HOST-003 新装恢复

- [x] 1.1 冻结源码和请求现场，阅读新旧恢复入口并由 Astra/high 给出提交语义与清理边界审查。
- [x] 1.2 实现 SQL 结果记录、锁内恢复与 CLI 分流；通过无数据库故障注入验证事务失败、提交未知、部署失败及恢复失败。
- [x] 1.3 验证候选或现场漂移、重复及并发恢复被正确处理，独立审查确认没有跨插件删除路径。
- [ ] 1.4 在获得精确隔离写库授权后执行真实 PostgreSQL 故障回归；提供旧冻结现场官方操作说明并由消费方复测。（专项真实回归已通过；冻结现场完整恢复仍待消费方确认。）

## 2. HOST-004 缓存并发

- [x] 2.1 以中立文件缓存夹具复现竞态，实现宿主缓存适配器并验证不改 vendor。
- [x] 2.2 完成 40 进程、至少 1,000 轮冷/热并发回归，覆盖标签过期、清理、损坏重建及角色隔离。
- [x] 2.3 固定候选后独立审查缓存行为和锁边界；在消费方受控拉取后补齐三个首屏接口的 1,000 轮真实 HTTP/JWT 验收。

## 3. 交付

- [x] 3.1 在执行记录分别写明两项根因、最小修复、验证结果和未验证条件；通过 OpenSpec 校验与 diff 审查。
- [x] 3.2 提交范围内改动并核实工作树 clean，提供消费方应拉取的 revision；初次交付未推送，后续按用户要求推送开发分支，不部署、不修改消费方。
