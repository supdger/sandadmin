## 1. 宿主目录契约

- [x] 1.1 为插件目录条目增加受限 `repository` 字段并以其构造 Release 地址，验证独立仓库下载、旧目录回退及非法仓库拒绝测试通过
- [x] 1.2 更新目录类型、示例、宿主治理和分发文档，验证不再声明业务插件源码或 Release 属于 SandAdmin
- [x] 1.3 更新三个现有目录条目指向独立仓库，运行 PHP 契约测试、前端类型检查、构建和 OpenSpec 严格校验

## 2. 独立插件仓库

- [x] 2.1 从 `sand-plugins` 已提交干净 revision 拆分 SandIAM 历史到 `supdger/sand-iam`，核对根目录交付物、来源提交和公开可读性
- [x] 2.2 从同一干净基线拆分 SandWorkflow 历史到 `supdger/sand-workflow`，核对许可证、NOTICE、根目录交付物和公开可读性
- [x] 2.3 从同一干净基线拆分 SandAI 插件历史到 `supdger/sand-ai`，确认不包含私有 `sand_ai` 应用仓库内容并核对公开可读性
- [x] 2.4 在三个独立仓库发布当前目录锁定的不可变 ZIP，逐一从远端下载并核对 SHA-256

## 3. 切换与清理

- [x] 3.1 先发布支持独立仓库的 SandAdmin main，再验证线上目录逐项解析到正确仓库且文档、候选下载不接受客户端 URL
- [ ] 3.2 在可操作的本地后台刷新仓库，验证三个插件卡片、版本详情和文档读取；记录安装或升级业务链仍未覆盖的边界
- [x] 3.3 删除 SandAdmin 中目录已不引用且独立仓库摘要一致的重复插件 Release 与标签，回读确认无旧引用
- [x] 3.4 更新 `sand-plugins` 权威来源和消费者基线记录，不覆盖其未提交工作；提交并推送各仓库后记录最终 revision
