## 1. 固定变更范围

- [x] 1.1 记录当前 revision、未提交补丁及同文件写者，核对 vendor 与锁定上游内容；产出逐文件差异表，覆盖数据库、宿主兼容、旧状态过渡和已复现缺陷四类。
- [x] 1.2 清点恢复 API/CLI、TerminalRunner 回调、自动依赖安装及前端入口的调用方；在设计中补充准确路径和切换条件，验证没有未分类的对外能力变化。

## 2. 恢复上游主流程

- [x] 2.1 基于锁定上游收敛 InstallLogic，保持 SandAdmin 标识和路径；通过逐段 diff 证明文件部署和依赖流程对应上游，正常路径不调用 FailedUpgrade 协调器。
- [x] 2.2 将三类生命周期 SQL 调用接入现有 PostgreSQL 执行器；运行引号、注释、dollar quote、事务指令及失败传播的针对性测试，验证不隐式建库。
- [x] 2.3 对齐控制器和异步依赖回调，保持现有管理入口；通过回调成功/失败测试验证完成状态和错误不会丢失。
- [x] 2.4 将旧状态检查限制在进入变更前，隔离旧恢复入口；测试状态 6/7/8 和残留候选不执行 SQL、不覆盖文件、不自动改成功。
- [x] 2.5 核对安全差异并仅保留有复现依据的必要补丁；用未授权访问和 ZIP 越界载荷验证拒绝行为，检查未修改 vendor。

## 3. 通用生命周期验证

- [x] 3.1 准备中立插件 v1/v2 及 SQL 失败变体；检查上游包结构和 PostgreSQL SQL，确认不要求恢复描述符，不引入持久业务插件副本。
- [x] 3.2 在获授权的独立宿主完成零插件基础运行、安装、页面加载、保留数据升级、卸载、卸载后登录；记录锁定 revision、包摘要、命令和可观察结果。
- [x] 3.3 在同一隔离环境验证 SQL 失败回滚和旧状态拒绝；证明未误报成功，并明确文件与数据库事务的边界。
- [x] 3.4 固定候选后按编排政策进行独立生命周期审查；记录实际执行证据及未验证层，不以静态测试替代真实宿主结果。

## 4. 兼容与交付

- [x] 4.1 修订恢复、依赖安装和前端激活公共文档及相关测试预期；核对与新流程一致，不继续承诺未实现自动激活。
- [x] 4.2 中立链通过后，在消费者授权范围验证 SandIAM 包兼容；产出具体兼容结论或违反的包契约，禁止同时修改两侧来掩盖问题。
- [x] 4.3 提供旧状态处理、候选切换和回退说明；审查确认删除、同步、SQL 执行、启停及发布各自有授权后才执行对应动作。

## 执行证据

当前验证结果、已知旧测试差异和未授权运行范围见 [执行记录](execution.md)。未完成项不以源码或离线测试替代实际验收。
