## 1. 仓库与安装

- [x] 1.1 实现配置、清单校验、受限下载和候选衔接，行为测试覆盖成功、错误身份、校验失败及兼容拒绝。
- [x] 1.2 替换商店代理路由，验证超级管理员约束与下载仅POST。
- [x] 1.3 提供打包命令、空清单和发布说明，通过临时插件打包并读取产物验证。

- [x] 1.4 支持发布契约明确声明的插件本地运行依赖，校验源码与最终ZIP的数量和树摘要；测试未声明、篡改及危险路径拒绝。

## 2. 界面与交付

- [x] 2.1 依独立设计替换商店界面，保留恢复闸门，通过类型检查和构建。
- [x] 2.2 更新同仓发布边界及安装说明，严格验证OpenSpec。
- [x] 2.3 独立审查候选并进行可用环境下的界面验收，记录真实网络、数据库和发布的未验证边界。

执行证据见 execution.md。2.3 的独立源码审查、隔离真实安装及HTTP验证通过；本轮浏览器恢复后完成3006真实界面验收。未将浏览器直接安装或升级视为已验证。

## 3. 状态识别、直接操作与文档

- [x] 3.1 后端返回真实安装状态及版本动作，下载预检提前拒绝；覆盖缺失运行目录和旧闸门。
- [x] 3.2 实现校验后的只读README接口，覆盖大小、身份、编码、无候选写入边界。
- [x] 3.3 插件管理正确显示动作；仓库一键串联安装升级及文档查看，通过类型和构建检查。
- [x] 3.4 在运行实例复验状态、文档和操作闸门，完成独立审查并记录真实UI/生命周期证据边界。

3.4：真实状态与README接口、独立源码及3006页面验收通过。浏览器直接安装、升级及真正失败升级恢复操作仍未演练；31918健康已安装界面停在验证码登录，未覆盖。

## 4. 用户截图暴露的页面纠偏

- [x] 4.1 修正普通禁写状态被误分类为数据库升级失败，增加真实分类回归并保留恢复闸门。
- [x] 4.2 仓库首屏只保留插件浏览与动作，管理页异常简短呈现，恢复和环境详情按需展开。
- [x] 4.3 独立操作真实页面验证缺失目录状态、默认折叠、文档及页面尺寸，并留存截图证据。

## 5. 持久安装存储与实际目录发现

- [x] 5.1 统一持久存储根与旧根兼容，明确双根冲突，接通普通/旧恢复/CLI路径。
- [x] 5.2 合并安装登记与实际插件目录，覆盖未登记、文件缺失、损坏元数据与只读边界。
- [x] 5.3 实现显式维护迁移检查和执行，验证锁占用、恢复记录、路径绑定拒绝及成功原子迁移。
- [x] 5.4 执行相关回归与独立审查，更新消费契约并通知sand_plugins跟进，记录实际应用边界。

## 6. 异常清理与重新安装

- [x] 6.1 实现受限清理预览、显式确认及持久续作，回归旧记录、保护对象、漂移和失败重试。
- [x] 6.2 管理界面提供清理入口、范围展示、确认、防重与成功刷新，完成类型检查及构建。
- [x] 6.3 独立审查并验证隔离真实数据库清理后重新安装、目标页面主流程与失败反馈，记录未覆盖边界。
- [x] 6.4 更新使用说明和执行证据，按既有授权提交推送并通知插件消费者。

## 7. 插件权限菜单清理兼容

- [x] 7.1 支持同插件受限 slug/component/path 条件，保持菜单归属链与完整子节点检查，覆盖越界与空 code 反例。
- [x] 7.2 独立复核及真实隔离 PostgreSQL 验证空 code 权限节点清理、其他菜单保留与重装。
- [x] 7.3 更新契约并提交推送，通知插件侧按锁定版本消费；实际插件包验收单独记录。

## 8. 用户界面与菜单修正

- [x] 8.1 调整仓库卡片标题、状态及操作层次，独立浏览器验证长文本与窄屏。
- [x] 8.2 移除两个安装脚本的默认文档外链，核实旧实例记录并定向处理。
- [x] 8.3 查明 IAM 孤儿权限来源，由权威插件源修复对象动作命名与父子映射，记录存量数据处理边界。
- [x] 8.4 更新验收证据并按既有授权提交推送和通知消费者。

- [ ] 8.5 取得当前实例数据迁移明确授权后应用已审042，复核100权限归位/95更名、215标识及20543角色关联保持，并浏览器验收菜单树。
