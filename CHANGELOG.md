# 变更记录

本文件从 SandAdmin 开源发布准备阶段开始维护。它不追溯性重写 SaiAdmin 或其他上游项目的历史；继承来源和授权见 [NOTICE](NOTICE)。

## Unreleased

- 后续 SandAdmin 版本线不再跟随 SaiAdmin `6.x` 版号，当前开发基线调整为 `0.1.0`；在完成更大范围的安装、升级、插件生命周期和业务链验证前，不作 1.0 稳定性承诺。
- 恢复 `server/` 完整 Webman 后端项目，SandAdmin 主线重新采用完整前后端源码仓库安装。
- 独立消费工作区接管 SandAdmin 与 Sand 插件的统一演示和验收宿主职责。
- `6.1.5` Composer 后端包保留为历史兼容发行；该历史编号不代表当前项目成熟度，当前主线也不再继续发布新的 Composer 后端包。

## 6.1.5 - 2026-09-21

- 发布 Packagist 稳定版，默认安装命令为 `composer require supdger/sandadmin`。
- 删除仓库过程目录，将插件目录收敛为根级 `catalog.json`；源码根只保留后端、前端、文档和发布必需文件。
- 宿主运行版本与 Composer 发布版本统一为 `6.1.5`，现有插件目录兼容上限同步到该版本。
- 全新标准 Webman 消费项目已验证无需自定义 VCS 仓库即可安装并加载 SandPackage 命令。
- 本次发布未创建数据库、执行迁移或重放登录与真实插件生命周期；这些仍是独立运行验收层。

## 6.1.5-rc.1 - 2026-09-21

- 后端改为 Composer library，由标准 Webman 消费项目安装 `sandadmin` 与 `sandpackage` 载荷。
- 源码仓库移除 Webman 运行骨架、重复测试和根级维护样板，保留后端载荷、Vue 前端、公共文档及兼容目录。
- 独立消费工作区成为 SandAdmin 与 Sand 插件统一演示消费者；源码与演示副本保持单向关系。
- 隔离安装、重复安装、卸载、配置保护、命令加载、后端契约、前端生产构建和独立审查通过。
- 本候选未在本轮创建数据库、执行迁移或重放登录与真实插件生命周期，因此发布为预发布版本。

### SandPackage

- 增加 PostgreSQL 新装失败的官方检查及受确认恢复入口，保留提交未知状态，继续部署不重跑 SQL。
  真实冻结现场恢复须另行完成。
- 正常安装链回归锁定的 SaiPackage 6.0.2，实现集中 PostgreSQL SQL 适配；旧恢复实现隔离保留。
  新普通候选不要求恢复清单，前端同步支持新的候选判断。当前仅完成源码和离线检查，尚未完成真实独立宿主验收。
- `6.1.5` 修复安装完成后登记摘要未刷新、下一次升级在校验前移动当前插件包的问题。
- 增加绑定逐文件清单和摘要确认的升级前备份恢复入口；该入口不执行 SQL、文件部署或服务重载，并支持中断后安全重试。
- 定义插件管理端源码、构建、激活和真实页面验证的独立状态契约；安全激活执行器仍在实现中，不能仅凭 `state=installed` 宣称生产前端已发布。

### 宿主缓存

- 文件缓存通过宿主驱动扩展保护多进程读写与标签复合操作，损坏标签安全失效重建；
  无数据库的中立进程回归与真实 HTTP/JWT 验收分别记录。

### 文档与发布准备

- 明确 SandAdmin 是基于 SaiAdmin 6.x 修改和维护的独立 PostgreSQL fork，且不是上游官方发行版。
- 建立宿主文档入口、首次安装说明、架构/插件契约、贡献与安全说明。
- 为 SandWorkflow 增加独立发布 README，并统一其用户/进度文档的当前版本标记为 `1.0.7`。

正式版本发布后，按版本号、发布日期和可验证的变更内容新增章节。不要把未经过真实环境验收的变更记录为已发布功能。
